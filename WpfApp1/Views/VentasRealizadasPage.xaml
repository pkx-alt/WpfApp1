<Page x:Class="WpfApp1.Views.VentasRealizadasPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:vm="clr-namespace:WpfApp1.ViewModels"
      xmlns:local="clr-namespace:WpfApp1.Views"
      mc:Ignorable="d" 
      d:DesignHeight="700" d:DesignWidth="1000"
      Title="VentasRealizadasPage"
      Background="{StaticResource PageBackground}">

    <Page.DataContext>
        <vm:VentasRealizadasViewModel/>
    </Page.DataContext>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Historial de ventas" FontSize="28" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
            <TextBlock Text="Consulta, reimpresión y análisis de tickets." FontSize="14" Foreground="{StaticResource TextSecondary}" Margin="0,0,0,20"/>

            <Border Background="{StaticResource SurfaceBrush}" 
                    CornerRadius="8" 
                    Padding="20,20,20,10" 
                    Margin="0,0,0,20" 
                    BorderBrush="{StaticResource BorderBrush}" 
                    BorderThickness="1">

                <WrapPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBox x:Name="TxtBusquedaHistorial" Width="250" Margin="0,0,15,10"
                             Text="{Binding TextoBusqueda, UpdateSourceTrigger=PropertyChanged}"
                             GotFocus="TxtBusquedaHistorial_GotFocus"
                             LostFocus="TxtBusquedaHistorial_LostFocus"/>

                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,15,10">
                        <TextBlock Text="Del:" VerticalAlignment="Center" Margin="0,0,5,0" Foreground="{StaticResource TextSecondary}"/>
                        <DatePicker SelectedDate="{Binding FechaDesde}" Width="120"/>
                        <TextBlock Text="al" VerticalAlignment="Center" Margin="10,0" Foreground="{StaticResource TextSecondary}"/>
                        <DatePicker SelectedDate="{Binding FechaHasta}" Width="120"/>
                    </StackPanel>

                    <ComboBox Width="200" Margin="15,0,0,10"
                              DisplayMemberPath="RazonSocial"
                              ItemsSource="{Binding ListaClientes}"
                              SelectedItem="{Binding ClienteSeleccionado}"/>
                </WrapPanel>
            </Border>
        </StackPanel>

        <DataGrid Grid.Row="1" 
                  ItemsSource="{Binding Ventas}">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Folio" Binding="{Binding Folio}" Width="80" FontWeight="Bold"/>
                <DataGridTextColumn Header="Cliente" Binding="{Binding Cliente}" Width="2*"/>
                <DataGridTextColumn Header="Fecha" Binding="{Binding FechaHora, StringFormat='dd/MM/yyyy HH:mm'}" Width="140" Foreground="{StaticResource TextSecondary}"/>

                <DataGridTextColumn Header="Prods." Binding="{Binding Productos}" Width="80">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="Total" Binding="{Binding Total, StringFormat=C}" Width="120" FontWeight="Bold" Foreground="{StaticResource PrimaryColor}"/>

                <DataGridTemplateColumn Header="Acciones" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Content="Ver" Margin="2" Padding="10,5" FontSize="12"
                                        Style="{StaticResource BtnSecondary}"
                                        Command="{Binding DataContext.VerDetalleCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                        CommandParameter="{Binding}"/>

                                <Button Content="Imprimir" Margin="2" Padding="10,5" FontSize="12"
                                        Style="{StaticResource BtnSecondary}"
                                        ToolTip="Reimprimir Ticket"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Margin="0,20,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel>
                        <TextBlock Text="{Binding ResumenTitulo}" FontSize="16" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                        <TextBlock Text="Métricas basadas en los filtros actuales" FontSize="12" Foreground="{StaticResource TextSecondary}"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="Filtrar por Depto:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="SemiBold" Foreground="{StaticResource TextSecondary}"/>
                        <ComboBox x:Name="CmbCategoriaFilter" Width="180"
                                  DisplayMemberPath="Nombre"
                                  ItemsSource="{Binding ListaCategorias}"
                                  SelectedItem="{Binding SelectedCategoria}"/>
                    </StackPanel>
                </Grid>

                <Separator Grid.Row="0" VerticalAlignment="Bottom" Margin="0,0,0,-5" Background="{StaticResource BorderBrush}"/>

                <UniformGrid Grid.Row="1" Columns="4" Margin="0,15,0,0">

                    <StackPanel>
                        <TextBlock Text="Ingresos Totales" FontSize="12" Foreground="{StaticResource TextSecondary}" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding IngresosTotales, StringFormat=C}" FontSize="24" FontWeight="Bold" Foreground="{StaticResource PrimaryColor}"/>
                    </StackPanel>

                    <StackPanel>
                        <TextBlock Text="Ganancia Estimada" FontSize="12" Foreground="{StaticResource TextSecondary}" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding GananciaTotal, StringFormat=C}" FontSize="24" FontWeight="Bold" Foreground="{StaticResource SuccessColor}"/>
                    </StackPanel>

                    <StackPanel>
                        <TextBlock Text="Ventas Realizadas" FontSize="12" Foreground="{StaticResource TextSecondary}" FontWeight="SemiBold"/>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding NumVentas}" FontSize="24" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                            <TextBlock Text=" tickets" VerticalAlignment="Bottom" Margin="5,0,0,4" Foreground="{StaticResource TextSecondary}"/>
                        </StackPanel>
                    </StackPanel>

                    <StackPanel>
                        <TextBlock Text="Ticket Promedio" FontSize="12" Foreground="{StaticResource TextSecondary}" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding TicketPromedio, StringFormat=C}" FontSize="24" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                    </StackPanel>

                </UniformGrid>
            </Grid>
        </Border>

    </Grid>
</Page>