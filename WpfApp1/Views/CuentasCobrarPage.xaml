<Page x:Class="WpfApp1.Views.CuentasCobrarPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="900"
      Title="Cuentas Por Cobrar"
      Background="{StaticResource PageBackground}">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Text="Cuentas por Cobrar" FontSize="28" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                <TextBlock Text="Gestión de créditos y abonos de clientes." FontSize="14" Foreground="{StaticResource TextSecondary}" Margin="0,5,0,0"/>
            </StackPanel>

            <Button Grid.Column="1" 
                    Command="{Binding RefrescarCommand}" 
                    Content="🔄 Actualizar" 
                    Style="{StaticResource BtnSecondary}"
                    Height="38"/>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="350"/>
                <ColumnDefinition Width="30"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="{StaticResource SurfaceBrush}" 
                    CornerRadius="8" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="15">
                        <TextBlock Text="Clientes con Deuda" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                    </Border>

                    <DataGrid Grid.Row="1" 
                              ItemsSource="{Binding ListaDeudores}" 
                              SelectedItem="{Binding DeudorSeleccionado, Mode=TwoWay}"
                              BorderThickness="0">

                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Cliente" Binding="{Binding NombreCompleto}" Width="*" FontWeight="SemiBold"/>

                            <DataGridTextColumn Header="Tickets" Binding="{Binding NumeroDeVentasPendientes}" Width="70">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Center"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <DataGridTextColumn Header="Total" Binding="{Binding TotalDeuda, StringFormat=C}" Width="90">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Right"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="Foreground" Value="{StaticResource DangerColor}"/>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                        <Setter Property="Margin" Value="0,0,10,0"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource CardStyle}" Padding="0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="#F8F9FA" CornerRadius="10,10,0,0" Padding="20" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
                        <StackPanel>
                            <TextBlock Text="Detalle de Adeudos" FontSize="18" FontWeight="Bold" Foreground="{StaticResource PrimaryColor}"/>
                            <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                <TextBlock Text="Cliente seleccionado:" Foreground="{StaticResource TextSecondary}"/>
                                <TextBlock Text="{Binding DeudorSeleccionado.NombreCompleto, TargetNullValue='(Ninguno)'}" 
                                           FontWeight="Bold" Margin="5,0,0,0" Foreground="{StaticResource TextPrimary}"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <DataGrid Grid.Row="1" 
          ItemsSource="{Binding MovimientosCliente}"
          SelectedItem="{Binding MovimientoSeleccionado}"
          BorderThickness="0" Margin="10"
          AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
          HeadersVisibility="Column" GridLinesVisibility="Horizontal"
          HorizontalGridLinesBrush="#EEE" VerticalGridLinesBrush="Transparent"
          RowHeight="40">

                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Fecha" Binding="{Binding Fecha, StringFormat=dd/MM/yyyy}" Width="100" Foreground="{StaticResource TextSecondary}"/>

                            <DataGridTextColumn Header="Folio" Binding="{Binding Folio}" Width="*" FontWeight="SemiBold"/>

                            <DataGridTextColumn Header="Importe" Binding="{Binding MontoOriginal, StringFormat=C}" Width="120">
                                <DataGridTextColumn.HeaderStyle>
                                    <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource {x:Type DataGridColumnHeader}}">
                                        <Setter Property="HorizontalContentAlignment" Value="Right"/>
                                    </Style>
                                </DataGridTextColumn.HeaderStyle>
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Right"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="Margin" Value="0,0,10,0"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <DataGridTextColumn Header="Abonado" Binding="{Binding PagadoHastaAhora, StringFormat=C}" Width="120" Foreground="{StaticResource SuccessColor}">
                                <DataGridTextColumn.HeaderStyle>
                                    <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource {x:Type DataGridColumnHeader}}">
                                        <Setter Property="HorizontalContentAlignment" Value="Right"/>
                                    </Style>
                                </DataGridTextColumn.HeaderStyle>
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Right"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="Margin" Value="0,0,10,0"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <DataGridTextColumn Header="Pendiente" Binding="{Binding SaldoPendiente, StringFormat=C}" Width="120" FontWeight="Bold" Foreground="{StaticResource DangerColor}">
                                <DataGridTextColumn.HeaderStyle>
                                    <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource {x:Type DataGridColumnHeader}}">
                                        <Setter Property="HorizontalContentAlignment" Value="Right"/>
                                    </Style>
                                </DataGridTextColumn.HeaderStyle>
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Right"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="Margin" Value="0,0,10,0"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>

                    <Border Grid.Row="2" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0" Padding="20">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="TOTAL A PAGAR" FontSize="12" FontWeight="Bold" Foreground="{StaticResource TextSecondary}"/>
                                <TextBlock Text="{Binding TotalSeleccionado, StringFormat=C}" 
                                           FontSize="32" FontWeight="ExtraBold" Foreground="{StaticResource DangerColor}"/>
                            </StackPanel>

                            <Button Grid.Column="1" 
                                    Command="{Binding RealizarAbonoCommand}" 
                                    Content="Realizar Abono ($)" 
                                    Style="{StaticResource BtnSuccess}"
                                    Height="50" Padding="30,0" FontSize="16">
                                <Button.Effect>
                                    <DropShadowEffect Color="#28A745" BlurRadius="10" Opacity="0.3" ShadowDepth="3"/>
                                </Button.Effect>
                            </Button>
                        </Grid>
                    </Border>
                </Grid>
            </Border>

        </Grid>
    </Grid>
</Page>