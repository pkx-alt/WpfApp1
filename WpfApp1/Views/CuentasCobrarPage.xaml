<Page x:Class="WpfApp1.Views.EmpleadosPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="900"
      Title="Cuentas Por Cobrar"
      FontFamily="Segoe UI">

    <Grid Background="#F5F5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Text="Cuentas por Cobrar" FontSize="28" FontWeight="Bold" Foreground="#333"/>
                <TextBlock Text="Gestión de créditos y abonos" FontSize="14" Foreground="Gray"/>
            </StackPanel>

            <Button Grid.Column="1" Command="{Binding RefrescarCommand}" Content="🔄 Actualizar Lista" Padding="10,5" VerticalAlignment="Center"/>
        </Grid>

        <Grid Grid.Row="1" Margin="20,0,20,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.3*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="White" CornerRadius="5" Padding="10" Margin="0,0,10,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Clientes con Deuda" FontWeight="SemiBold" Margin="0,0,0,10"/>

                    <DataGrid Grid.Row="1" 
                              ItemsSource="{Binding ListaDeudores}" 
                              SelectedItem="{Binding DeudorSeleccionado, Mode=TwoWay}"
                              AutoGenerateColumns="False" 
                              CanUserAddRows="False" IsReadOnly="True"
                              SelectionMode="Single"
                              HeadersVisibility="Column" GridLinesVisibility="Horizontal">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Cliente" Width="*" Binding="{Binding NombreCompleto}"/>
                            <DataGridTextColumn Header="Tickets" Width="Auto" Binding="{Binding NumeroDeVentasPendientes}"/>
                            <DataGridTextColumn Header="Total" Width="Auto" Binding="{Binding TotalDeuda, StringFormat=C}" FontWeight="Bold" Foreground="Red"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <Border Grid.Column="1" Background="White" CornerRadius="5" Padding="20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Margin="0,0,0,15">
                        <TextBlock Text="Detalle de Adeudos" FontSize="18" FontWeight="Bold" Foreground="#007ACC"/>
                        <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                            <TextBlock Text="Cliente: " FontWeight="Bold"/>
                            <TextBlock Text="{Binding DeudorSeleccionado.NombreCompleto, TargetNullValue='Seleccione un cliente...'}" Foreground="Gray"/>
                        </StackPanel>
                    </StackPanel>

                    <DataGrid Grid.Row="1" 
                              ItemsSource="{Binding MovimientosCliente}"
                              AutoGenerateColumns="False" 
                              CanUserAddRows="False" IsReadOnly="True"
                              HeadersVisibility="Column" Background="WhiteSmoke">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Fecha" Binding="{Binding Fecha, StringFormat=dd/MM/yyyy}"/>
                            <DataGridTextColumn Header="Folio" Binding="{Binding Folio}"/>
                            <DataGridTextColumn Header="Total Venta" Binding="{Binding MontoOriginal, StringFormat=C}"/>
                            <DataGridTextColumn Header="Pagado" Binding="{Binding PagadoHastaAhora, StringFormat=C}"/>
                            <DataGridTextColumn Header="Saldo Restante" Binding="{Binding SaldoPendiente, StringFormat=C}" FontWeight="Bold" Foreground="#D9534F"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <Grid Grid.Row="2" Margin="0,20,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="Total Deuda Cliente: " FontSize="16"/>
                            <TextBlock Text="{Binding TotalSeleccionado, StringFormat=C}" FontSize="20" FontWeight="Bold" Foreground="Red" Margin="5,0,0,0"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <Button Command="{Binding RealizarAbonoCommand}" 
                                    Content="REALIZAR ABONO" 
                                    Padding="20,10" 
                                    Background="#007ACC" Foreground="White" FontWeight="Bold" Cursor="Hand"/>
                        </StackPanel>
                    </Grid>
                </Grid>
            </Border>

        </Grid>
    </Grid>
</Page>