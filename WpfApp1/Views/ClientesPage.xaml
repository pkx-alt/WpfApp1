<Page x:Class="OrySiPOS.Views.ClientesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:OrySiPOS.Views"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="900"
      Title="ClientesPage"
      Background="{StaticResource PageBackground}">

    <Page.Resources>
        <ContextMenu x:Key="MenuAccionesCliente">
            <MenuItem Header="✏️ Editar Cliente" 
              Click="MenuEditarCliente_Click" />

            <MenuItem Header="📄 Ver Detalles" Click="VerDetallesMenu_Click"/>

            <Separator/>

            <MenuItem Click="MenuCambiarEstado_Click">
                <MenuItem.Style>
                    <Style TargetType="MenuItem">
                        <Setter Property="Header" Value="🚫 Deshabilitar"/>
                        <Setter Property="Foreground" Value="{StaticResource DangerColor}"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Activo}" Value="False">
                                <Setter Property="Header" Value="✅ Habilitar"/>
                                <Setter Property="Foreground" Value="{StaticResource SuccessColor}"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </MenuItem.Style>
            </MenuItem>
        </ContextMenu>
    </Page.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="Clientes" FontSize="28" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                    <TextBlock Text="Gestión de cartera de clientes" FontSize="14" Foreground="{StaticResource TextSecondary}"/>
                </StackPanel>

                <Button Grid.Column="1" 
                        x:Name="btnNuevoCliente"
                        Content="+ Nuevo Cliente" 
                        Style="{StaticResource BtnSuccess}"
                        Click="btnNuevoCliente_Click"/>
            </Grid>

            <Border Background="{StaticResource SurfaceBrush}" 
                    CornerRadius="8" 
                    Padding="20,20,20,10" 
                    Margin="0,0,0,20" 
                    BorderBrush="{StaticResource BorderBrush}" 
                    BorderThickness="1">

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <WrapPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Grid VerticalAlignment="Center" Margin="0,0,20,10">
                            <TextBox x:Name="txtBusqueda" Width="300"
                 Text="Buscar cliente por RFC o razón social"
                 Foreground="Gray"
                 TextChanged="txtBusqueda_TextChanged"
                 GotFocus="txtBusqueda_GotFocus"     
                 LostFocus="txtBusqueda_LostFocus"/>
                        </Grid>

                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,10">
                            <TextBlock Text="Estatus:" VerticalAlignment="Center" Foreground="Gray" Margin="0,0,5,0" FontSize="11"/>
                            <CheckBox x:Name="chkActivos" Content="Activos" IsChecked="True" Margin="0,0,10,0" Click="chkStatus_Click"/>
                            <CheckBox x:Name="chkInactivos" Content="Inactivos" Click="chkStatus_Click"/>

                            <Border Width="1" Height="20" Background="#DDD" Margin="15,0"/>

                            <TextBlock Text="Tipo:" VerticalAlignment="Center" Foreground="Gray" Margin="0,0,5,0" FontSize="11"/>
                            <CheckBox x:Name="chkConFactura" Content="Facturados" IsChecked="True" Margin="0,0,10,0" Click="chkStatus_Click"/>
                            <CheckBox x:Name="chkSinFactura" Content="Público Gral." IsChecked="True" Click="chkStatus_Click"/>
                        </StackPanel>
                    </WrapPanel>

                    <StackPanel x:Name="spAccionesSeleccion" 
                                Grid.Column="1" 
                                Orientation="Horizontal" 
                                Visibility="Collapsed" 
                                VerticalAlignment="Center"
                                Margin="0,0,0,10">

                        <TextBlock x:Name="txtConteoSeleccion" 
                                   Text="0 seleccionados" 
                                   VerticalAlignment="Center" 
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource TextSecondary}"
                                   Margin="0,0,15,0"/>

                        <Button x:Name="btnDeshabilitarSeleccion" 
                                Content="Deshabilitar Selección" 
                                Style="{StaticResource BtnWarning}"
                                Click="btnDeshabilitarSeleccion_Click"/>
                    </StackPanel>
                </Grid>
            </Border>
        </StackPanel>

        <DataGrid x:Name="GridClientes"
          Grid.Row="1"
          ItemsSource="{Binding Clientes}"
          MouseDoubleClick="GridClientes_MouseDoubleClick">
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow" BasedOn="{StaticResource {x:Type DataGridRow}}">
                    <Setter Property="ContextMenu" Value="{StaticResource MenuAccionesCliente}"/>
                </Style>
            </DataGrid.RowStyle>

            <DataGrid.Columns>

                <DataGridTemplateColumn Width="40">
                    <DataGridTemplateColumn.Header>
                        <CheckBox x:Name="chkSeleccionarTodo" Click="chkSeleccionarTodo_Click" ToolTip="Seleccionar todo"/>
                    </DataGridTemplateColumn.Header>
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" 
                                      HorizontalAlignment="Center" VerticalAlignment="Center"
                                      Checked="ClienteCheckBox_Changed" Unchecked="ClienteCheckBox_Changed"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="ID" Binding="{Binding ID}" Width="50">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="RFC" Binding="{Binding RFC}" Width="130" FontWeight="SemiBold"/>

                <DataGridTextColumn Header="Razón Social" Binding="{Binding RazonSocial}" Width="*">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                            <Setter Property="Padding" Value="5,0"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="Teléfono" Binding="{Binding Telefono}" Width="110"/>

                <DataGridTemplateColumn Header="Estado" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border CornerRadius="4" Padding="8,2" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="#D4EDDA"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Activo}" Value="False">
                                                <Setter Property="Background" Value="#F8D7DA"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>

                                <TextBlock FontWeight="Bold" FontSize="11">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="ACTIVO"/>
                                            <Setter Property="Foreground" Value="#155724"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Activo}" Value="False">
                                                    <Setter Property="Text" Value="INACTIVO"/>
                                                    <Setter Property="Foreground" Value="#721C24"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Registro" Binding="{Binding Creado, StringFormat='dd/MM/yy'}" Width="90" Foreground="{StaticResource TextSecondary}">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTemplateColumn Header="Tipo" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border Visibility="{Binding EsFactura, Converter={StaticResource BooleanToVisibilityConverter}}"
                                    Background="#E3F2FD" 
                                    CornerRadius="4" 
                                    Padding="5,2" 
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center">
                                <TextBlock Text="📄 CFDI" 
                                           Foreground="{StaticResource PrimaryColor}" 
                                           FontWeight="Bold" 
                                           FontSize="10"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="Acciones" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="..." 
                    Width="30" Height="30" Padding="0" 
                    HorizontalAlignment="Center"
                    Style="{StaticResource BtnSecondary}"
                    Click="BtnOpciones_Click"
                    ToolTip="Más acciones"
                    ContextMenu="{StaticResource MenuAccionesCliente}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

            </DataGrid.Columns>
        </DataGrid>
        <Border Grid.Row="2" 
        Background="#E9ECEF" 
        BorderBrush="{StaticResource BorderBrush}" 
        BorderThickness="0,1,0,0" 
        Padding="15,5" 
        Margin="0,10,0,0"
        CornerRadius="0,0,8,8">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock Text="Total clientes:" FontWeight="SemiBold" Foreground="{StaticResource TextSecondary}" Margin="0,0,5,0"/>
                <TextBlock Text="{Binding TotalClientes}" FontWeight="Bold" Foreground="{StaticResource PrimaryColor}"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>