<Page x:Class="OrySiPOS.Views.IngresosPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:OrySiPOS.Views"
      xmlns:vm="clr-namespace:OrySiPOS.ViewModels"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="900"
      Title="IngresosPage"
      Background="{StaticResource PageBackground}">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="Gestión de Ingresos" FontSize="28" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                <TextBlock Text="Historial de entradas de dinero y ajustes." FontSize="14" Foreground="{StaticResource TextSecondary}" Margin="0,5,0,0"/>
            </StackPanel>

            <Border Grid.Column="1" Style="{StaticResource CardStyle}" Padding="20,10" Margin="20,0,0,0" MinWidth="250">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Total Ingresos (Mes Actual)" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource TextSecondary}" HorizontalAlignment="Right"/>
                    <TextBlock Text="{Binding TotalIngresosMes, StringFormat=C}" 
                               FontSize="28" FontWeight="Bold" Foreground="{StaticResource SuccessColor}" 
                               HorizontalAlignment="Right"/>
                </StackPanel>
            </Border>
        </Grid>

        <Border Grid.Row="1" Background="{StaticResource SurfaceBrush}" 
                CornerRadius="8" Padding="20,20,20,10" Margin="0,0,0,20"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">

            <WrapPanel Orientation="Horizontal" VerticalAlignment="Center">

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,20,10">
                    <TextBlock Text="Del:" VerticalAlignment="Center" Margin="0,0,5,0" Foreground="{StaticResource TextSecondary}"/>
                    <DatePicker Width="120" SelectedDate="{Binding FechaInicio}"/>
                    <TextBlock Text="al" VerticalAlignment="Center" Margin="10,0" Foreground="{StaticResource TextSecondary}"/>
                    <DatePicker Width="120" SelectedDate="{Binding FechaFin}"/>
                </StackPanel>

                <Grid VerticalAlignment="Center" Margin="0,0,20,10">
                    <TextBox Width="220" 
                             Text="{Binding TextoBusqueda, UpdateSourceTrigger=PropertyChanged}" 
                             Tag="Buscar por concepto..."/>
                </Grid>

                <Button x:Name="BtnRegistrarIngreso" 
                        Click="BtnRegistrarIngreso_Click"
                        Content="+ Registrar Ingreso"
                        Style="{StaticResource BtnSuccess}"
                        Height="38" Padding="20,0"
                        Margin="20,0,0,10"/>
            </WrapPanel>
        </Border>

        <DataGrid Grid.Row="2" x:Name="GridIngresos" 
          ItemsSource="{Binding IngresosFiltrados}"
          MouseDoubleClick="GridIngresos_MouseDoubleClick">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Fecha" Binding="{Binding Fecha, StringFormat='dd MMM yyyy'}" Width="120" Foreground="{StaticResource TextSecondary}"/>

                <DataGridTemplateColumn Header="Categoría" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border Background="#D4EDDA" CornerRadius="4" Padding="8,2" HorizontalAlignment="Left">
                                <TextBlock Text="{Binding Categoria}" Foreground="{StaticResource SuccessColor}" FontWeight="SemiBold" FontSize="11"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Descripción" Binding="{Binding Concepto}" Width="*"/>

                <DataGridTextColumn Header="Usuario" Binding="{Binding Usuario}" Width="120" Foreground="{StaticResource TextSecondary}"/>

                <DataGridTextColumn Header="Monto" Binding="{Binding Monto, StringFormat=C}" Width="120" FontWeight="Bold" Foreground="{StaticResource SuccessColor}">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Right"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTemplateColumn Header="" Width="50">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="..." Style="{StaticResource BtnSecondary}" Padding="0" Width="30" Height="30" ToolTip="Ver detalle"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        <Border Grid.Row="3" Background="#E9ECEF" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0" Padding="15,5" Margin="0,10,0,0" CornerRadius="0,0,8,8">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock Text="Registros encontrados:" FontWeight="SemiBold" Foreground="{StaticResource TextSecondary}" Margin="0,0,5,0"/>
                <TextBlock Text="{Binding TotalIngresos}" FontWeight="Bold" Foreground="{StaticResource PrimaryColor}"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>