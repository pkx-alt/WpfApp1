<Page x:Class="OrySiPOS.Views.CotizacionesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:vm="clr-namespace:OrySiPOS.ViewModels"
      xmlns:local="clr-namespace:OrySiPOS.Views"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800"
      Title="CotizacionesPage"
      Background="{StaticResource PageBackground}">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="Cotizaciones" FontSize="28" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                    <TextBlock Text="Historial y gestión de presupuestos" FontSize="14" Foreground="{StaticResource TextSecondary}"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">

                    <Button Content="☁️ Sincronizar" 
            Command="{Binding SincronizarWebCommand}"
            Style="{StaticResource BtnSecondary}" 
            Margin="0,0,10,0"/>

                    <Button Content="+ Nueva cotización"
            Style="{StaticResource BtnSuccess}" 
            Click="BtnNuevaCotizacion_Click"/>

                </StackPanel>
            </Grid>

            <Border Background="{StaticResource SurfaceBrush}" 
                    CornerRadius="8" 
                    Padding="20,20,20,10" 
                    Margin="0,0,0,20" 
                    BorderBrush="{StaticResource BorderBrush}" 
                    BorderThickness="1">

                <WrapPanel Orientation="Horizontal" VerticalAlignment="Center">

                    <TextBox x:Name="txtBusqueda" Width="250" Margin="0,0,15,10"
                             Text="Buscar por folio, cliente...."
                             Foreground="Gray"
                             TextChanged="txtBusqueda_TextChanged"
                             GotFocus="txtBusqueda_GotFocus"      
                             LostFocus="txtBusqueda_LostFocus"/>

                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,15,10">
                        <TextBlock Text="Desde:" VerticalAlignment="Center" Margin="0,0,5,0" Foreground="{StaticResource TextSecondary}"/>
                        <DatePicker Width="130" SelectedDate="{Binding FechaDesde}"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,15,10">
                        <TextBlock Text="Hasta:" VerticalAlignment="Center" Margin="0,0,5,0" Foreground="{StaticResource TextSecondary}"/>
                        <DatePicker Width="130" SelectedDate="{Binding FechaHasta}"/>
                    </StackPanel>

                    <ComboBox Width="180" Margin="0,0,15,10"
                              DisplayMemberPath="RazonSocial"
                              ItemsSource="{Binding ListaClientes}"
                              SelectedItem="{Binding ClienteSeleccionado}"/>

                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,15,10">
                        <TextBlock Text="Origen:" VerticalAlignment="Center" Margin="0,0,5,0" Foreground="{StaticResource TextSecondary}"/>
                        <ComboBox Width="100" 
              ItemsSource="{Binding ListaOrigenes}"
              SelectedItem="{Binding OrigenSeleccionado}"/>
                    </StackPanel>

                </WrapPanel>
            </Border>
        </StackPanel>

        <DataGrid Grid.Row="1" 
          ItemsSource="{Binding ListaCotizaciones}"
          AutoGenerateColumns="False"
          MouseDoubleClick="DataGrid_MouseDoubleClick">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Folio" Binding="{Binding Folio}" Width="80" FontWeight="Bold"/>

                <DataGridTextColumn Header="Emisión" Binding="{Binding FechaEmision, StringFormat=dd/MM/yyyy}" Width="120" Foreground="{StaticResource TextSecondary}"/>

                <DataGridTextColumn Header="Cliente" Binding="{Binding ClienteNombre}" Width="2*"/>

                <DataGridTextColumn Header="Vence" Binding="{Binding FechaVencimiento, StringFormat=dd/MM/yyyy}" Width="120"/>

                <DataGridTextColumn Header="Total" Binding="{Binding Total, StringFormat=C}" Width="120" FontWeight="Bold" Foreground="{StaticResource PrimaryColor}"/>

                <DataGridTextColumn Header="Estado" Binding="{Binding Estado}" Width="100"/>

                <DataGridTextColumn Header="Origen" Binding="{Binding Origen}" Width="80">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Origen}" Value="Web">
                                    <Setter Property="Foreground" Value="{StaticResource PrimaryColor}"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTemplateColumn Header="Acciones" Width="180">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Content="Ver" Margin="2" Padding="10,5" FontSize="12" 
                                        Style="{StaticResource BtnSecondary}"
                                        Click="BtnVerDetalle_Click"
                                        Tag="{Binding Folio}"/>

                                <Button Content="Imprimir" Margin="2" Padding="10,5" FontSize="12"
                                    Style="{StaticResource BtnSecondary}"
                                    ToolTip="Imprimir Ticket de Cotización"
                                    Command="{Binding DataContext.ImprimirTicketCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                    CommandParameter="{Binding}" />
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        <Border Grid.Row="2" 
        Background="#E9ECEF" 
        BorderBrush="{StaticResource BorderBrush}" 
        BorderThickness="0,1,0,0" 
        Padding="15,5" 
        Margin="0,10,0,0"
        CornerRadius="0,0,8,8">

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock Text="Total cotizaciones:" 
                   FontWeight="SemiBold" 
                   Foreground="{StaticResource TextSecondary}" 
                   Margin="0,0,5,0"/>

                <TextBlock Text="{Binding TotalCotizaciones}" 
                   FontWeight="Bold" 
                   Foreground="{StaticResource PrimaryColor}"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>