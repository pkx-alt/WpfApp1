<Page x:Class="WpfApp1.Views.CotizacionesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.expression/blend/2008" 
      xmlns:vm="clr-namespace:WpfApp1.ViewModels"
      xmlns:local="clr-namespace:WpfApp1.Views"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800"
      Title="CotizacionesPage"
      Background="White">

    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">

            <Grid Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Vertical">

                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Text="Cotizaciones" 
                                   FontSize="24" 
                                   FontWeight="Bold"
                                   VerticalAlignment="Center"/>

                        <Button Grid.Column="1" 
                            Content="+ Nueva cotización"
                            Background="#005A9E"
                            Foreground="White"
                            FontWeight="Bold"
                            Padding="10,5"
                            BorderThickness="0"
        
                            Click="BtnNuevaCotizacion_Click">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="3"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                    </Grid>

                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBox Grid.Column="0" 
                             x:Name="txtBusqueda" 
                             Padding="5" 
                             VerticalContentAlignment="Center"
                             Text="Buscar por folio, cliente...."
                             Foreground="Gray"
                             FontSize="14"
                             TextChanged="txtBusqueda_TextChanged"
                             GotFocus="txtBusqueda_GotFocus"      
                             LostFocus="txtBusqueda_LostFocus"/>        
                    </Grid>

                    <WrapPanel Margin="0,0,0,10">

                        <StackPanel Orientation="Horizontal" Margin="0,5,20,5">
                            <TextBlock Text="Desde" VerticalAlignment="Center" Margin="0,0,5,0"/>
                            <DatePicker SelectedDate="{Binding FechaDesde}"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Margin="0,5,20,5">
                            <TextBlock Text="Hasta" VerticalAlignment="Center" Margin="0,0,5,0"/>
                            <DatePicker SelectedDate="{Binding FechaHasta}"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Margin="0,5,20,5">
                            <TextBlock Text="Cliente" VerticalAlignment="Center" Margin="0,0,5,0"/>
                            <ComboBox Width="150"
                              DisplayMemberPath="RazonSocial"
                              ItemsSource="{Binding ListaClientes}"
                              SelectedItem="{Binding ClienteSeleccionado}"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Margin="0,5,20,5">
                            <TextBlock Text="Origen:" VerticalAlignment="Center" Margin="0,0,5,0" FontWeight="SemiBold"/>
                            <ComboBox Width="120" SelectedItem="{Binding OrigenSeleccionado}">
                                <ComboBoxItem Content="Todos" IsSelected="True"/>
                            </ComboBox>
                        </StackPanel>

                    </WrapPanel>

                </StackPanel>

                <DataGrid Grid.Row="1" 
                          AutoGenerateColumns="False" 
                          ItemsSource="{Binding ListaCotizaciones}" IsReadOnly="True"
                          CanUserAddRows="False"
                          VerticalGridLinesBrush="Transparent"
                          HorizontalGridLinesBrush="#EEE"
                          Margin="0,10,0,10">

                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#FFDCC2"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Padding" Value="10,5"/>
                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                            <Setter Property="BorderThickness" Value="0,0,1,0"/>
                            <Setter Property="BorderBrush" Value="White"/>
                        </Style>
                    </DataGrid.Resources>

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Folio" Binding="{Binding Folio}" Width="Auto" FontWeight="Bold"/>
                        <DataGridTextColumn Header="Emisión" Binding="{Binding FechaEmision, StringFormat=dd/MM/yyyy}" Width="Auto"/>
                        <DataGridTextColumn Header="Cliente" Binding="{Binding ClienteNombre}" Width="2*"/>
                        <DataGridTextColumn Header="Vence" Binding="{Binding FechaVencimiento, StringFormat=dd/MM/yyyy}" Width="Auto"/>
                        <DataGridTextColumn Header="Total" Binding="{Binding Total, StringFormat=C}" Width="*"/>
                        <DataGridTextColumn Header="Estado" Binding="{Binding Estado}" Width="Auto"/>

                        <DataGridTemplateColumn Header="Acciones" Width="Auto">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">

                                        <Button Content="Ver" Margin="5" ToolTip="Ver detalle"
                        Click="BtnVerDetalle_Click"
                        Tag="{Binding Folio}"/>

                                        <Button Content="Imprimir" Margin="5" ToolTip="Imprimir ticket"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

            </Grid>
        </ScrollViewer>
    </Grid>
</Page>