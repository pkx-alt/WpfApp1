<Window x:Class="OrySiPOS.Views.Dialogs.AperturaCajaWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Apertura de Caja" 
        Height="Auto" Width="400"
        SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        MouseDown="Window_MouseDown">

    <Border Background="{StaticResource PageBackground}" 
            CornerRadius="15" 
            BorderBrush="{StaticResource BorderBrush}" 
            BorderThickness="1" 
            Margin="10">

        <Border.Effect>
            <DropShadowEffect Color="#B0BEC5" BlurRadius="20" ShadowDepth="5" Opacity="0.4"/>
        </Border.Effect>

        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,25">
                <Border Background="#E8F5E9" CornerRadius="50" Width="70" Height="70" Margin="0,0,0,15">
                    <TextBlock Text="🔓" FontSize="32" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <TextBlock Text="Apertura de Turno" FontSize="22" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" HorizontalAlignment="Center"/>
                <TextBlock x:Name="FechaHoraTextBlock" Text="Cargando fecha..." FontSize="13" Foreground="{StaticResource TextSecondary}" HorizontalAlignment="Center" Margin="0,5,0,0"/>
            </StackPanel>

            <Border Grid.Row="1" Background="White" CornerRadius="12" Padding="25" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="Fondo Inicial en Efectivo" FontWeight="SemiBold" Foreground="{StaticResource TextSecondary}" Margin="0,0,0,10"/>

                    <Border BorderBrush="{StaticResource PrimaryColor}" BorderThickness="1.5" CornerRadius="8" Background="#FAFAFA" Height="60">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="$" 
                                       FontSize="26" 
                                       FontWeight="Bold" 
                                       Foreground="{StaticResource TextSecondary}" 
                                       VerticalAlignment="Center" 
                                       Margin="15,0,5,3"
                                       Opacity="0.6"/>

                            <TextBox x:Name="MontoInicialTextBox" 
                                     Grid.Column="1"
                                     FontSize="32" 
                                     FontWeight="Bold" 
                                     Foreground="{StaticResource TextPrimary}"
                                     Background="Transparent" 
                                     BorderThickness="0" 
                                     Padding="0"
                                     VerticalAlignment="Center"
                                     VerticalContentAlignment="Center"
                                     Text="0.00">
                                <TextBox.Template>
                                    <ControlTemplate TargetType="TextBox">
                                        <ScrollViewer x:Name="PART_ContentHost" VerticalAlignment="Center"/>
                                    </ControlTemplate>
                                </TextBox.Template>
                            </TextBox>
                        </Grid>
                    </Border>

                    <TextBlock Text="Ingresa el dinero base en caja para cambios." FontSize="12" Foreground="{StaticResource TextSecondary}" Margin="2,8,0,0" FontStyle="Italic" Opacity="0.8"/>
                </StackPanel>
            </Border>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,30,0,0">
                <Button x:Name="CancelarButton" Content="Cancelar" 
                        Style="{StaticResource BtnSecondary}" 
                        Height="45" Width="110" 
                        Click="CancelarButton_Click" 
                        Margin="0,0,10,0"
                        IsCancel="True"/>

                <Button x:Name="FinalizarButton" Content="Abrir Caja" 
                        Style="{StaticResource BtnSuccess}" 
                        Height="45" Width="140" 
                        FontSize="15"
                        Click="FinalizarButton_Click">
                    <Button.Effect>
                        <DropShadowEffect Color="#28A745" BlurRadius="10" Opacity="0.3"/>
                    </Button.Effect>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>