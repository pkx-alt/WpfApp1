<Window x:Class="WpfApp1.Views.Dialogs.HistorialCajaWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Historial de Cortes de Caja" Height="500" Width="800"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Historial de Cortes 📅" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <DataGrid x:Name="GridHistorial" Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                  HeadersVisibility="Column" GridLinesVisibility="Horizontal" Background="White">
            <DataGrid.Columns>
                <DataGridTextColumn Header="#" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Apertura" Binding="{Binding FechaApertura, StringFormat='dd/MM/yy HH:mm'}" Width="120"/>
                <DataGridTextColumn Header="Cierre" Binding="{Binding FechaCierre, StringFormat='dd/MM/yy HH:mm'}" Width="120"/>

                <DataGridTextColumn Header="Inicial" Binding="{Binding MontoInicial, StringFormat=C}" Width="100"/>
                <DataGridTextColumn Header="Final" Binding="{Binding MontoFinal, StringFormat=C}" Width="100" FontWeight="Bold"/>

                <DataGridTextColumn Header="Diferencia" Binding="{Binding Diferencia, StringFormat=C}" Width="100">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Diferencia}" Value="0">
                                    <Setter Property="Foreground" Value="Green"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="Notas" Binding="{Binding Notas}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <Button Grid.Row="2" Content="Cerrar" HorizontalAlignment="Right" Margin="0,15,0,0" Width="100" Height="30" Click="Button_Click"/>
    </Grid>
</Window>