<Window x:Class="WpfApp1.Views.Dialogs.DescuentoDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1.Views.Dialogs"
        mc:Ignorable="d"
        Title="Aplicar Descuento" 
        SizeToContent="WidthAndHeight" 
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        FontFamily="Segoe UI"
        Background="#F5F5F5"
        WindowStyle="ToolWindow"
        KeyDown="Window_KeyDown">

    <Border Padding="20">
        <StackPanel Width="280">
            <TextBlock Text="Valor del Descuento:" FontWeight="SemiBold" FontSize="14"/>
            <TextBox x:Name="ValorTextBox"
                     Margin="0,8,0,0"
                     Padding="8"
                     FontSize="16"
                     PreviewTextInput="ValorTextBox_PreviewTextInput"/>

            <TextBlock Text="Tipo de Descuento:" FontWeight="SemiBold" FontSize="14" Margin="0,15,0,0"/>
            <StackPanel Margin="0,8,0,0">
                <RadioButton x:Name="PorcentajeRadioButton" 
                             Content="Porcentaje (%)" 
                             GroupName="TipoDescuento" 
                             IsChecked="True" 
                             FontSize="14"/>
                <RadioButton x:Name="MontoFijoRadioButton" 
                             Content="Monto Fijo ($)" 
                             GroupName="TipoDescuento" 
                             Margin="0,8,0,0"
                             FontSize="14"/>
            </StackPanel>

            <Separator Margin="0,20,0,15"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button x:Name="CancelarButton"
                        Grid.Column="0"
                        Content="Cancelar"
                        Padding="10,8"
                        Margin="0,0,5,0"
                        Click="CancelarButton_Click"/>

                <Button x:Name="AceptarButton"
                        Grid.Column="1"
                        Content="Aceptar"
                        Padding="10,8"
                        Margin="5,0,0,0"
                        IsDefault="True"
                        Background="#007ACC"
                        Foreground="White"
                        FontWeight="Bold"
                        Click="AceptarButton_Click"/>
            </Grid>
        </StackPanel>
    </Border>
</Window>