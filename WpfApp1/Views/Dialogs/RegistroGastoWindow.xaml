<Window x:Class="WpfApp1.Dialogs.RegistroGastoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Registrar Nuevo Gasto" Height="450" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        FontFamily="Segoe UI">

    <Window.Resources>
        <SolidColorBrush x:Key="ColorPrimario" Color="#D32F2F"/>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Concepto / Descripción:" Margin="0,0,0,5" FontWeight="SemiBold"/>
            <TextBox Text="{Binding NuevoGasto.Concepto, UpdateSourceTrigger=PropertyChanged}" Height="30" Margin="0,0,0,15"/>

            <TextBlock Text="Categoría:" Margin="0,0,0,5" FontWeight="SemiBold"/>
            <ComboBox ItemsSource="{Binding CategoriasDisponibles}" 
                      SelectedItem="{Binding NuevoGasto.Categoria}"
                      Height="30" Margin="0,0,0,15"/>

            <TextBlock Text="Monto:" Margin="0,0,0,5" FontWeight="SemiBold"/>
            <TextBox Text="{Binding NuevoGasto.Monto, UpdateSourceTrigger=PropertyChanged, StringFormat=N2}" Height="30" Margin="0,0,0,15"/>

            <TextBlock Text="Método de Pago:" Margin="0,0,0,5" FontWeight="SemiBold"/>
            <ComboBox ItemsSource="{Binding MetodosDisponibles}"
                      SelectedItem="{Binding NuevoGasto.MetodoPago}"
                      Height="30" Margin="0,0,0,15"/>

            <TextBlock Text="Registrado Por (Usuario):" Margin="0,0,0,5" FontWeight="SemiBold"/>
            <TextBox Text="{Binding NuevoGasto.Usuario}" Height="30" Margin="0,0,0,15"/>

            <TextBlock Text="Fecha (por defecto hoy):" Margin="0,0,0,5" FontWeight="SemiBold"/>
            <DatePicker SelectedDate="{Binding NuevoGasto.Fecha}" Height="30" Margin="0,0,0,15"/>

        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">

            <Button Content="Cancelar" Width="90" Height="35" Margin="0,0,10,0" Background="Transparent" Foreground="Gray" BorderBrush="Gray" Cursor="Hand"/>

            <Button Content="Guardar Gasto" Width="120" Height="35" Background="{StaticResource ColorPrimario}" Foreground="White" BorderThickness="0" Cursor="Hand"
                    Command="{Binding GuardarGastoCommand}"/>
        </StackPanel>
    </Grid>
</Window>