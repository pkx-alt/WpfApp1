<Window x:Class="WpfApp1.Views.ResumirVentaDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1.Views"
        mc:Ignorable="d"
        Title="ResumirVentaDialog" Height="450" Width="800">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Ventas en Espera"
               FontSize="20" FontWeight="SemiBold"
               Margin="0,0,0,15"/>

        <ListBox Grid.Row="1" x:Name="SesionesListBox"
             Margin="0,0,0,15"
             FontSize="14">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Resumen}" Padding="10"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal"
                HorizontalAlignment="Right">

            <Button x:Name="AbrirButton" Content="Abrir Venta" 
                FontWeight="Bold" Padding="15,8"
                Click="AbrirButton_Click"/>

            <Button Content="Cancelar" Padding="15,8" Margin="10,0,0,0"
                IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
