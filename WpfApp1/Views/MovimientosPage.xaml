<Page x:Class="OrySiPOS.Views.MovimientosPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:OrySiPOS.Views"
      xmlns:converters="clr-namespace:OrySiPOS.Converters"  
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="900"
      Title="MovimientosPage" 
      Background="{StaticResource PageBackground}" 
      FontFamily="Segoe UI">

    <Page.Resources>
        <converters:StringContainsConverter x:Key="StringContainsConverter"/>
    </Page.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                <TextBlock Text="{Binding TituloPagina}" 
                           FontSize="28" FontWeight="Bold" 
                           Foreground="{StaticResource TextPrimary}"/>
                <TextBlock Text="Historial detallado de entradas, salidas y ajustes."
                           FontSize="14" 
                           Foreground="{StaticResource TextSecondary}" 
                           Margin="0,5,0,0"/>
            </StackPanel>

            <Button Grid.Column="1" 
                    Content="⬅ Volver al Inventario"
                    Click="BtnVolver_Click"
                    Style="{StaticResource BtnSecondary}"/>
        </Grid>

        <DataGrid Grid.Row="1" 
                  ItemsSource="{Binding ListaMovimientos}">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Fecha" Binding="{Binding Fecha, StringFormat='dd/MM/yyyy HH:mm'}" Width="140"/>

                <DataGridTextColumn Header="Producto" Binding="{Binding Producto.Descripcion}" Width="2*"/>

                <DataGridTemplateColumn Header="Movimiento" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding TipoMovimiento}" FontWeight="SemiBold">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>

                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding TipoMovimiento, Converter={StaticResource StringContainsConverter}, ConverterParameter='Entrada'}" Value="True">
                                                <Setter Property="Foreground" Value="{StaticResource SuccessColor}"/>
                                            </DataTrigger>

                                            <DataTrigger Binding="{Binding TipoMovimiento, Converter={StaticResource StringContainsConverter}, ConverterParameter='Salida'}" Value="True">
                                                <Setter Property="Foreground" Value="{StaticResource DangerColor}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Cant." Binding="{Binding Cantidad}" Width="80" FontWeight="Bold"/>

                <DataGridTextColumn Header="Antes" Binding="{Binding StockAnterior}" Width="80" Foreground="{StaticResource TextSecondary}"/>
                <DataGridTextColumn Header="Después" Binding="{Binding StockNuevo}" Width="80" FontWeight="Bold"/>

                <DataGridTextColumn Header="Motivo / Nota" Binding="{Binding Motivo}" Width="2*"/>
                <DataGridTextColumn Header="Usuario" Binding="{Binding Usuario}" Width="100" Foreground="{StaticResource TextSecondary}"/>

            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Row="2" 
        Background="#E9ECEF" 
        BorderBrush="{StaticResource BorderBrush}" 
        BorderThickness="0,1,0,0" 
        Padding="15,5" 
        Margin="0,10,0,0"
        CornerRadius="0,0,8,8">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock Text="Movimientos registrados:" FontWeight="SemiBold" Foreground="{StaticResource TextSecondary}" Margin="0,0,5,0"/>
                <TextBlock Text="{Binding TotalMovimientos}" FontWeight="Bold" Foreground="{StaticResource PrimaryColor}"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>